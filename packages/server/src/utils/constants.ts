export enum ROUTES {
  USER = '/user',
  NOMINATION = '/nomination',
  MAILING_LIST = '/mailingList',
  MEMBERSHIP = '/membership',
  VOTING = '/voting',
  POSITION = '/position',
}

export enum MONGO_ERRORS {
  DUPLICATE_KEY = 11000,
}
